using System.Windows;

namespace {{namespace}}.Views
{
    /// <summary>
    /// {{name}}.xaml에 대한 상호 작용 로직
    /// </summary>
    public partial class {{name}} : Window
    {
        public {{name}}()
        {
            InitializeComponent();
            DataContext = new {{namespace}}.ViewModels.{{name}}ViewModel();
        }
    }
}
