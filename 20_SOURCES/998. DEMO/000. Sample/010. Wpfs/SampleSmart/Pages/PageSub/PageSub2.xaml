<UserControl x:Class="SampleSmart.Pages.PageSub.PageSub2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Background="#FFF3F7FF">
        <Border Background="White"
                BorderBrush="#CCD7EE"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                Width="520"
                Height="300"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>
                <TextBlock Text="📘 PageSub2"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0 0 0 12"/>

                <TextBlock Text="{Binding Message}"
                           FontSize="16"
                           Foreground="#555"
                           Margin="0 0 0 24"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Notice 열기"
                            Width="110"
                            Height="32"
                            Margin="0 0 8 0"
                            Command="{Binding OpenNoticeCommand}"/>

                    <Button Content="확인"
                            Width="90"
                            Height="32"
                            Command="{Binding OkCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>