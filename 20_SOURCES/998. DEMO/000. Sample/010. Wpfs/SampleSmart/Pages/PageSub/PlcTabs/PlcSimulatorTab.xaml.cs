using System.Windows.Controls;

namespace SampleSmart.Pages.PageSub.PlcTabs;

/// <summary>
/// \if KO
/// <para>\brief PLC Simulator TCP 테스트 탭입니다.</para>
/// \endif
/// \if EN
/// <para>Encapsulates plc simulator tab functionality and related state.</para>
/// \endif
/// </summary>
public partial class PlcSimulatorTab : UserControl
{
    /// <summary>
    /// \if KO
    /// <para>\brief PlcSimulatorTab 클래스의 새 인스턴스를 초기화합니다.</para>
    /// \endif
    /// \if EN
    /// <para>Initializes a new instance of the <see cref="PlcSimulatorTab"/> class with the specified settings.</para>
    /// \endif
    /// </summary>
    public PlcSimulatorTab()
    {
        InitializeComponent();
    }
}
