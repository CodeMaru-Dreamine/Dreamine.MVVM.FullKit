using System.Windows.Controls;

namespace SampleSmart.Pages.PageSub;

/// <summary>
/// \if KO
/// <para>Page Database 기능과 관련 상태를 캡슐화합니다.</para>
/// \endif
/// \if EN
/// <para>Interaction logic for PageDatabase.xaml.</para>
/// \endif
/// </summary>
public partial class PageDatabase : UserControl
{
    /// <summary>
    /// \if KO
    /// <para>지정한 설정으로 <see cref="PageDatabase"/> 클래스의 새 인스턴스를 초기화합니다.</para>
    /// \endif
    /// \if EN
    /// <para>Initializes a new instance of the <see cref="PageDatabase"/> class with the specified settings.</para>
    /// \endif
    /// </summary>
    public PageDatabase()
    {
        InitializeComponent();
    }
}
