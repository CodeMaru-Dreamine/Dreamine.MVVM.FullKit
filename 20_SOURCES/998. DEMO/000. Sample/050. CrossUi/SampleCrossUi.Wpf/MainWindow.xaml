<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="SampleCrossUi.Wpf.MainWindow"
        Title="Dreamine Cross-UI - WPF"
        Width="480" Height="540"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Dreamine Cross-UI Sample - WPF"
                   FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,16" />

        <TextBlock Grid.Row="1"
                   Text="{Binding Count, StringFormat='Count: {0}'}"
                   FontSize="32"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
            <Button Content="Increment"
                    Command="{Binding IncrementCommand}"
                    Width="100" Height="34" Margin="0,0,8,0" />
            <Button Content="Reset"
                    Command="{Binding ResetCommand}"
                    Width="100" Height="34" />
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Operation Log:" FontWeight="Bold" Margin="0,0,0,4" />

        <ListBox Grid.Row="4"
                 ItemsSource="{Binding Logs}"
                 BorderThickness="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding CreatedAt, StringFormat='[HH:mm:ss]', Mode=OneWay}" />
                        <Run Text=" " />
                        <Run Text="{Binding Message, Mode=OneWay}" />
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
